[package]
name = "fbz"
version = "0.1.12"
edition = "2024"
rust-version = "1.91"
license = "Apache-2.0"
description = "Fast parallel compression and decompression for bzip2, gzip, LZ4, tar, and ZIP"
repository = "https://github.com/AnswerDotAI/fbz"
homepage = "https://github.com/AnswerDotAI/fbz"
documentation = "https://docs.rs/fbz"
include = ["/src/**", "/Cargo.toml", "/README.md", "/DEV.md", "/LICENSE"]

[lib]
name = "fbz"
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "fbz"
path = "src/bin/fbz.rs"
test = false

[profile.release]
lto = false
codegen-units = 16

[profile.release.package.fbz]
incremental = true

[profile.dist]
inherits = "release"
lto = true
incremental = false
codegen-units = 1
strip = true

[profile.dist.package.fbz]
incremental = false

[dependencies]
blake3 = "1.8.7"
clap = { version = "4.6.6", features = ["derive"] }
crc32fast = "1.5.1"
memmap2 = "0.9.11"
pyo3 = { version = ">=0.29.2", optional = true }
rayon = "1.12.0"
rgapi = "0.1.27"
serde_json = "1.0.151"
tempfile = "3.27.0"
tar = { version = "0.4.46", default-features = false }
twox-hash = { version = "2.0.0", default-features = false, features = ["xxhash32"] }
zip = { version = "8.6.0", default-features = false }

[dev-dependencies]
crabz2 = { version = "0.4.0", features = ["parallel"] }
flate2 = { version = "1.1", default-features = false, features = ["rust_backend"] }
libc = "0.2.175"
libbz2-rs-sys = { version = "0.2.5", default-features = false, features = ["std"] }
lz4_flex = "0.14.0"

[features]
python = ["dep:pyo3"]
extension-module = ["python", "pyo3/extension-module"]
